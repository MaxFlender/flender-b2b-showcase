function format(first, middle, last) {
    return (first || '') + (middle ? ` ${middle}` : '') + (last ? ` ${last}` : '');
}
const EMPTY_STRING = '';
const HASHTAG = '#';

export { EMPTY_STRING as E, HASHTAG as H, format as f };

//# sourceMappingURL=p-3c82445c.js.map